{
  "name": "ct-optimization",
  "version": "1.7.0",
  "private": true,
  "description": "CT Optimization - WordPress plugin admin screen",
  "scripts": {
    "build": "wp-scripts build --webpack-src-dir=src/admin --output-path=admin/build",
    "start": "wp-scripts start --webpack-src-dir=src/admin --output-path=admin/build",
    "packages-update": "wp-scripts packages-update"
  },
  "devDependencies": {
    "@wordpress/scripts": "^30.0.0"
  }
}
